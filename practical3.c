#include <stdio.h>

// Name: Golu Kumar Singh
// Roll No: 2510990132

int main() {
    int a = 0, b = 0;
    int sum;

    // Input
    printf("Enter two integers separated by space: ");
    scanf("%d %d", &a, &b);

    // Processing
    sum = a + b;

    // Output
    printf("Sum of %d and %d is: %d\n", a, b, sum);

    return 0;
}
